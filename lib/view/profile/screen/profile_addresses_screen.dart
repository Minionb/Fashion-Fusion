import 'package:flutter/material.dart';

class ShippingAddresses extends StatelessWidget {
  const ShippingAddresses({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Nothing Done Yet");
  }
}