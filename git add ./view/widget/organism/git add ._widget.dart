import 'package:flutter/material.dart';

class gitAddWidget extends StatelessWidget {
  const gitAddWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}